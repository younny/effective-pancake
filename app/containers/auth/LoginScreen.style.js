import { StyleSheet } from 'react-native'
import { AppStyles } from '../../themes'

export default StyleSheet.create({
    ...AppStyles.screen,
    contentContainer: {
        flex: 1,
        padding: 10,
    }
})